settings:
  lockfile: false

formatter: "markdown"

recursive:
  enabled: true
  path: modules/aws

output:
  file: "TFDOCS.md"
  mode: replace
  template: |-
    {{ .Content }}
    {{/** End of file fixer */}}
